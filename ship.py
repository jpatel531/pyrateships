class Ship(object):

	def __init__(self, name):
		if name is "Bananaboat": self.length = 5
		if name is "Poirateshap": self.length = 4
		if name is "Poopdeck" or name is "Rumbarrel": self.length = 3
		if name is "Tuggy": self.length = 2 